#!/bin/bash

# Theme-aware accent: blue (dark) / magenta (light)
if [ -f "$HOME/.theme-mode" ] && [ "$(cat "$HOME/.theme-mode" | tr -d '[:space:]')" = "dark" ]; then
    accent=0xff0a7aca # blue
else
    accent=0xffbf68d9 # magenta
fi

options=(
    style=round
    width=10.0
    hidpi=off
    active_color=$accent
    inactive_color=0xff333333
)
borders "${options[@]}"
